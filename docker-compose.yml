services:
  k6:
    image: grafana/k6:latest
    container_name: k6
    command: run /tests/load-test.js
    volumes:
      - ./tests:/tests
    depends_on:
      - app

  app:
    build: .
    container_name: app
    ports:
      - "3000:3000"
